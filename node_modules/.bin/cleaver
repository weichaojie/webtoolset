#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cleaver/bin/cleaver" "$@"
  ret=$?
else 
  node  "$basedir/../cleaver/bin/cleaver" "$@"
  ret=$?
fi
exit $ret
